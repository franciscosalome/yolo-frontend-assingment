import styled from "styled-components";

export const Container = styled.section`
  
  #home__description {
      h1 {
        font-size: 2rem;
        font-weight: 400;
        margin-bottom: 1rem;
        color: var(--white);
      }
      h2 {
        font-size: 1.2rem;
        font-weight: 400;
        color: var(--secondary);
      }
    }
  #home__selected-coins{
    margin-top: 25px;
  }

`
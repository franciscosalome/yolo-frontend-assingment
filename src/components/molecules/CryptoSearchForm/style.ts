import styled from "styled-components";

export const Card = styled.div`
  width: 300px;
  background: var(--white);
  border-radius: 2px;
  padding: 2rem;

  > h6 {
    color: var(--secondary);
    font-size: 0.75rem;
    font-weight: 300;
    text-align: center;
    margin-top: 2rem;
  }
`